import { ViewTransitionDirective } from './view-transition.directive';

describe('ViewTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
